# Calling-MessageBox-From-Assembly-x64
A structural x64 assembly template demonstrating how to dynamically load native Windows DLLs and safely execute exported functions using the Microsoft x64 calling convention.
